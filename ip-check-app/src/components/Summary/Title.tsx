import styled from "@emotion/styled";

export const Title = styled.h3`
  color: var(--dark-gray);
  font-size: 14px;
  text-transform: uppercase;
  font-weight: 700;
  letter-spacing: 3px;
  margin-bottom: 15px;
`;
